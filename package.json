{
  "name": "vercel-api",
  "version": "1.0.0",
  "description": "API for generating user progress images",
  "main": "server.js",
  "scripts": {
    "start": "node /api/user.js",
    "dev": "nodemon /api/user.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "@napi-rs/canvas": "^0.1.23"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  },
  "engines": {
    "node": "18.x"
  }
}
